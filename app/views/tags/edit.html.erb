<% content_for :title, "Edit Tag — #{@tag.name}" %>
<div class="max-w-lg mx-auto">
  <h1 class="text-xl font-semibold text-navy tracking-[-0.1px] mb-8">Edit Tag</h1>
  <%= render "form", tag: @tag %>
</div>

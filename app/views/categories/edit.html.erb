<% content_for :title, "Edit Category — #{@category.name}" %>
<div class="max-w-lg mx-auto">
  <h1 class="text-xl font-semibold text-navy tracking-[-0.1px] mb-8">Edit Category</h1>
  <%= render "form", category: @category %>
</div>

<% content_for :title, "Edit Post — #{@post.title}" %>

<div class="max-w-2xl mx-auto">
  <h1 class="text-xl font-semibold text-navy tracking-[-0.1px] mb-8">Edit Post</h1>
  <%= render "form", post: @post %>
</div>
